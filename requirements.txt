# ── Core web framework ──────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
pydantic>=2.6.0

# ── Auth: OIDC/JWKS RS256 token verification (backend/auth.py) ───────────────
cryptography>=41.0.0

# ── Database ────────────────────────────────────────────────────────────────
asyncpg>=0.29.0

# ── HTTP clients ────────────────────────────────────────────────────────────
httpx>=0.27.0
boto3>=1.34.0          # AWS S3 + SQS for the ingestion worker
requests>=2.31.0       # legacy HTTP for vision_processor / parser

# ── ML / embeddings / generation ────────────────────────────────────────────
openai-whisper>=20231117      # local transcription
sentence-transformers>=2.6.0  # cross-encoder reranker
numpy>=1.24.0,<2.3.0

# ── Document parsing ─────────────────────────────────────────────────────────
pypdf>=4.1.0
PyMuPDF>=1.24.0
pillow>=10.2.0
pytesseract>=0.3.10

# ── Optional: ffmpeg-python for fine-grained video control ──────────────────
# (ffmpeg itself is installed in the Docker image; this is only needed if you
# want to drive ffmpeg from Python. backend.transcription uses subprocess.)
